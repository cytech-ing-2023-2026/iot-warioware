//| mill-version: 1.1.5
//| mvnDeps:
//| - "io.github.iltotore::mill-tailwind::0.1.1"

import mill.*, scalalib.*, scalajslib.*, scalafmt.*
import io.github.iltotore.tailwind.TailwindModule

object `package` extends ScalaJSModule, ScalafmtModule, TailwindModule:

  def scalaVersion = "3.8.2"

  def scalaJSVersion = "1.20.2"

  def tailwindVersion = "4.2.2"

  def mvnDeps = Seq(
    mvn"io.indigoengine::tyrian-zio::0.14.0",
    mvn"dev.zio::zio-interop-cats::23.1.0.3",
    mvn"dev.zio::zio-json::0.9.0"
  )

  override def moduleKind = Task(mill.scalajslib.api.ModuleKind.ESModule)
